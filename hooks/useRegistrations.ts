/**
 * Re-export barrel for backward compatibility.
 * All logic lives in hooks/registrations/.
 */
export { useRegistrations } from './registrations';
export type { Registration, StatusFilter, ShowAlert, UseRegistrationsReturn } from './registrations';
