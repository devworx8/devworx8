/**
 * Teacher Message Thread Screen
 * Consolidated: re-exports the unified ParentMessageThreadScreen.
 * Teacher, parent, and principal all share the same chat UI.
 */
import ParentMessageThreadScreen from './parent-message-thread';

export default ParentMessageThreadScreen;
